import React from 'react'

export default function RightContainerHeader(props) {
    return (
        <div>
            <h1 className="font-primary font-bold text-xl">{props.text}</h1>
        </div>
    )
}
