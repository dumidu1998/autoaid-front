import React from 'react'

export default function ForgetPasswordText(props) {
    return (
        <div>
            <p className="font-primary text-blue-500 ">{props.name}</p>
        </div>
    )
}
